<Window x:Class="JulioCode01.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JulioCode01.Views"
        xmlns:viewModels="clr-namespace:JulioCode06.ViewModels;assembly=JulioCode06.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32*"/>
            <RowDefinition Height="152*"/>
            <RowDefinition Height="33*"/>
        </Grid.RowDefinitions>
        <Button Content="Load trades" Command="{Binding LoadTradeCommand}" HorizontalAlignment="Left" Margin="309,32,0,0" VerticalAlignment="Top"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding TradesList}" />
    </Grid>
</Window>
